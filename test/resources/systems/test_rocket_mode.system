hosts:
    - hostname: 'thishost'
      hostgroup: 'thisgroup'
      ip_address: 127.0.0.1

parameters:
    deployuser: {{ sys_local_user }}
    sudouser: {{ sys_local_user }}
    rootuser: {{ sys_local_user }}
    sudo: yes

plans:
    - plan: rocketmode_test
